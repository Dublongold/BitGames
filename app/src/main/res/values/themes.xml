<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.BitGames" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.BitGames" parent="Base.Theme.BitGames" />
    <style name="Theme.BitGames.Web" parent="Theme.Material3.Dark.NoActionBar" />
</resources>